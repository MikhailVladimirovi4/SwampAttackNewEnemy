using UnityEngine;

public class AnimatorRocketController : MonoBehaviour
{
public static class Params
    {
       public const string ReachedTarget = nameof(ReachedTarget);
    }

    public static class State
    {
        public const string Blast = nameof(Blast);
    }
}
